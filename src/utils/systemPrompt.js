/**
 * System prompt for the Financial Comparison Assistant
 * Content is read from llm_prompt.md at build time and injected by Vite's define config.
 * Edit llm_prompt.md to change the prompt — restart the dev server to pick up changes.
 */
export const SYSTEM_PROMPT = __SYSTEM_PROMPT__;
